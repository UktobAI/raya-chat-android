jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.10-open
  - sdk use java 17.0.10-open

install:
  - ./gradlew :raya-chat-core:publishReleasePublicationToMavenLocal :raya-chat-ui:publishReleasePublicationToMavenLocal -x test
